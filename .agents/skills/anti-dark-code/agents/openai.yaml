interface:
  display_name: "Anti-Dark Code"
  short_description: "Map code truth and build deterministic verification"
  default_prompt: "Use $anti-dark-code for the requested outcome and scope. Select the matching task card, reuse checked evidence, perform authorized work, and report verification and coverage limits."

policy:
  allow_implicit_invocation: true
