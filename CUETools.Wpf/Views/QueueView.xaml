<UserControl x:Class="CUETools.Wpf.Views.QueueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="{StaticResource Ground}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- add bar -->
    <Border Grid.Row="0" Background="{StaticResource Panel}" BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="16,11">
      <DockPanel LastChildFill="False">
        <TextBlock DockPanel.Dock="Left" Text="Action" Foreground="{StaticResource Muted}" FontSize="11" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <ComboBox DockPanel.Dock="Left" Width="100" VerticalAlignment="Center" ItemsSource="{Binding Actions}" SelectedItem="{Binding SelectedAction, Mode=TwoWay}"/>
        <ComboBox DockPanel.Dock="Left" Width="80" VerticalAlignment="Center" Margin="8,0,0,0" ItemsSource="{Binding Formats}" SelectedItem="{Binding SelectedFormat, Mode=TwoWay}"/>
        <Button DockPanel.Dock="Left" Content="Add file(s)..." Command="{Binding AddFilesCommand}" Padding="12,6" Margin="14,0,0,0" VerticalAlignment="Center"/>
        <Button DockPanel.Dock="Left" Content="Add folder..." Command="{Binding AddFolderCommand}" Padding="12,6" Margin="8,0,0,0" VerticalAlignment="Center"/>

        <Button DockPanel.Dock="Right" Content="Run all" Command="{Binding RunAllCommand}" Style="{StaticResource Accent}" Padding="16,6" Margin="8,0,0,0" VerticalAlignment="Center"/>
        <Button DockPanel.Dock="Right" Content="Clear" Command="{Binding ClearCommand}" Padding="12,6" VerticalAlignment="Center"/>
      </DockPanel>
    </Border>

    <!-- queue list -->
    <Grid Grid.Row="1">
      <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource Muted}" FontFamily="{StaticResource Serif}" FontSize="18"
                 Text="Queue is empty - add album folders or .cue files above."
                 Visibility="{Binding Items.Count, Converter={StaticResource ZeroToVis}}"/>

      <ListView Margin="12" BorderThickness="0" Background="Transparent" ItemsSource="{Binding Items}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
        <ListView.View>
          <GridView>
            <GridViewColumn Header="Source" Width="300" DisplayMemberBinding="{Binding Display}"/>
            <GridViewColumn Header="Action" Width="90" DisplayMemberBinding="{Binding Action}"/>
            <GridViewColumn Header="Status" Width="110" DisplayMemberBinding="{Binding Status}"/>
            <GridViewColumn Header="Result" Width="320" DisplayMemberBinding="{Binding Result}"/>
          </GridView>
        </ListView.View>
      </ListView>
    </Grid>

    <!-- progress bar -->
    <Border Grid.Row="2" Background="{StaticResource Panel}" BorderBrush="{StaticResource Line}" BorderThickness="0,1,0,0" Padding="16,10">
      <StackPanel VerticalAlignment="Center">
        <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource InkDim}" FontSize="12" TextTrimming="CharacterEllipsis"/>
        <ProgressBar Height="4" Margin="0,7,0,0" Minimum="0" Maximum="1" Value="{Binding Progress, Mode=OneWay}"
                     Background="{StaticResource Glass}" Foreground="{StaticResource Teal}" BorderThickness="0"/>
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
